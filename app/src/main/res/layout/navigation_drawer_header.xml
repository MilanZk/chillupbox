<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/navigation_drawer_header_background"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="@dimen/main_drawer_header_padding"
    android:theme="@style/AppTheme.DrawerHeader">

    <ImageView
        android:id="@+id/iv_navigation_drawer_header_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_name"
        android:paddingTop="@dimen/main_drawer_header_padding"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <TextView
        android:id="@+id/tv_navigation_drawer_header_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/main_drawer_header_vertical_spacing"
        android:text="@string/navigation_drawer_header_title"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Body1" />

</LinearLayout>
