package com.zgz.mobile.android.prepexam.model.bufferoo

/**
 * Representation for a [Credentials] fetched from an external layer data source
 */
data class Credentials(val username: String, val id: String)